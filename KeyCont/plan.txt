  ===================
  25.09.02г. 17:43:31

  План развития программы:


 * UART0_A1x - переопределить функцию getchar для стандартного потока ввода
 * UART0_A1x - переопределить биты порта сигналов CTS, RTS, DTR, DSR
 UART0_A1x - переопределить сигнал CTS на бит порта, который
             фиксирует перепад уровня 0->1

 UART1_A1x - переопределить переменные для работы со вторым UART1


 KEY_CONTR_A2x -  используя стандартные потоки ввода и вывода сканировать
                  поток
 KEY_CONTR_A2x -  выделить форматированный ввод, сравнить со словарем
                  и выполнить необходимые функции вывода на дисплей или
                  ввод состояния клавиатуры
 KEY_CONTR_A2x - Режим тестирование индикаторов и светодиодов на панели
                 управленияю.

 KEY_CONTR_A2x - команды контроллеру запись настроек, вывод в индикаторы

 ===================
 01.10.02г. 11:18:45

 * Обмен с хост (управляющим контроллером)
   "Событие изменение состояния клавиатуры"
        - одновременное переключение выключателя, т.е. событие 0-1 и 1-0
    Необходимо отправить результат новое состояние клавиатуры и
    событие в соответствии с матрицами разрешения событий
 -> ДА

 -------------------
 01.10.02г. 15:25:42
 ? Словарь функций
        Структура словаря:

 |__   ИМЯ ФУНКЦИИ                 тип СТРОКА
 |    (проверка: 1-й символ не специальный знак или цифра)
 |__  КОЛИЧЕСТВО АРГУМЕНТОВ       тип СИМВОЛЬНАЯ 0-255
 |
 |__  ИНФОРМАЦИЯ                   тип СТРОКА
      ( краткая справка о функции выводить если ошибка )

      Модуль "СКАНЕР КОМАНД/ФУНКЦИЙ"

      ПЕРЕЧЕНЬ ФУНКЦИЙ

      test_cmd     проверить синтаксис и имя функции


 1.  |========================================================
     | "Функция 1" Начало
     | -------------------------------------------------------
     | ИМЯ ФУНКЦИИ: test_cmd
     |
     | ВХОДНЫЕ ПАРАМЕТРЫ:       указатели  buffer_cmd, dict_cmd
     | тип переменных           RAM buffer_cmd
     |                          ROM dict_cmd
     |
     | ИЗМЕНЯЕМЫЕ ПЕРЕМЕННЫЕ:   error_cmd, buffer_cmd
     | тип переменных           Char            error_cmd
     |                          Char struct     buffer_cmd
     |
     | переменная flagerror  - если равно 0, результат функции;
     |                         если не равно 0, сообщение об ошибке
     | буфер buffercmd       - результат функции или сообщение об
     |                         ошибке
     |
     | ВЫХОДНОЙ РЕЗУЛЬТАТ:     указатель на  buffer_cmd
     |
     |
     | НАЗНАЧЕНИЕ:
     |  Проверяет синтаксис функции, текст которой находится
     |  в буфере buffer_cmd
     |
     | ОПИСАНИЕ:
     |  - Указатели на буффер и словарь функций
     |  - Найти количество "(" в буфере
     |  - Найти количество ")" в буфере
     |  - Сообщение "Ошибка синтаксиса"
     |    условие: если Кол-во "(" не равно 1  или
     |                  Кол-во ")" не равно 1  или
     |  - Выделить "имя_функции"
     |  - Сообщение "Ошибка "НЕТ ФУНКЦИИ" "
     |    условие если "имя_функции" нет в словаре
     |  - Найти количество параметров функции в буфере, т.е ","
     |  - Сравнить со словарем функций
     |    условие: если ( кол-во "," + 1 ) не равно ("кол-во параметров" из
     |    словаря, то сообщение "Ошибка в аргументах"
     |
     | ============================================================
     | "Функция 1" Конец
     | ************************************************************










